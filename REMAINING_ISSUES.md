# 剩余问题列表

## ✅ 已修复

### 1. 段落评论无限循环（已修复 ✅）
- **问题**：40+次并发请求导致连接池耗尽
- **解决方案**：批量API，一次请求获取所有段落评论数
- **效果**：API请求 ↓97.5%，连接池使用 >100% → <5%

### 2. 阅读器点赞错误（已修复 ✅）
- **问题**：`TypeError: Cannot read properties of undefined (reading 'likeCount')`
- **根本原因**：API返回异常数据时前端崩溃
- **解决方案**：添加数据验证，确保 `data.likeCount` 和 `data.liked` 存在
- **效果**：不再崩溃，有详细错误日志

### 3. Redis 缓存未出现（已修复 ✅）
- **问题**：首页刷新多次，Upstash Data Browser 没有 `home:all-data` 缓存
- **根本原因**：`fetchCache = 'force-cache'` 阻止了 Redis SET 操作
- **详细分析**：
  1. `force-cache` 缓存所有 fetch（包括 Upstash Redis HTTP 请求）
  2. 第一次构建：Redis GET → MISS → Next.js 缓存这个"MISS"结果
  3. Redis SET 被跳过（因为 fetch 被缓存）
  4. 后续请求用 Next.js 缓存的结果，永远不调用 Redis
- **解决方案**：改用 `dynamic = 'force-dynamic'`
- **效果**：
  - ✅ 首页每次请求都执行
  - ✅ Redis 缓存正常工作
  - ✅ 第一次访问写入缓存，第二次访问命中缓存

---

## 🔧 待修复

### 4. Writer Dashboard - Recent Stories 更新不及时
- [ ] 已上传的小说没有显示
- [ ] 需要检查缓存失效机制

### 5. 官方账号 (ButterPicks) 问题
- [ ] 小说详情页点击官方账号没有显示官方卡片
- [ ] Tags 还是4个（应该有官方标识）
- [ ] 官方账号应该有官方头像边框
- [ ] 头像下面显示 "Novice Reader" 而不是 "Official"
- [ ] 官方账号 modal 只显示粉丝数，不显示 following/books read

### 6. 分页问题
- [ ] Profile 相关的 library 应该分页
- [ ] Reading history 应该分页

---

## 优先级

1. **P0（严重）** - ✅ 已全部修复
2. **P1（高）** - ✅ 已全部修复
3. **P2（中）** - Writer dashboard、官方账号
4. **P3（低）** - 分页

---

## 部署后验证

部署后检查 Vercel 日志应该看到：

```
[Redis GET] Fetching key: home:all-data
[Redis GET] Cache MISS (home:all-data)
[Redis SET] Writing key: home:all-data, TTL: 3600
[Redis SET] Success (home:all-data)

// 第二次访问：
[Redis GET] Fetching key: home:all-data
[Redis GET] Cache HIT (home:all-data)
```

检查 Upstash Data Browser 应该看到：
- ✅ Key: `home:all-data`
- ✅ TTL: 约 3600 秒（1小时）
